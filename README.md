# 🍃 Tea Delivery


##  Архитектура

Приложение построено с использованием **MVVM (Model-View-ViewModel)** архитектуры с элементами **Clean Architecture** и следует принципам **SOLID**:

### 📁 Структура проекта

```
lib/
├── app/                    # Общие компоненты приложения
│   ├── api_client/        # HTTP клиент ( ИМИТАЦИЯ!! )
│   ├── entity/            # Модели данных
│   ├── repositories/      # Репозитории для работы с данными
│   └── ui/                # Общие UI компоненты
├── features/              # Функциональные модули
│   ├── auth/              # Аутентификация
│   ├── cart/              # Корзина
│   ├── home/              # Главный экран
│   ├── product_details/   # Детали товара
│   └── product_list/      # Список товаров
├── router/                # Навигация и роутинг
└── resources/             # Ресурсы (изображения)
```



- **View** (Screens/Widgets) → отображает UI и получает пользовательский ввод
- **ViewModel** → обрабатывает бизнес-логику, управляет состоянием, взаимодействует с Repository
- **Model** (Entity) → представляет данные приложения
- **Repository** → абстракция для работы с данными (API, локальное хранилище)

## 📦 Зависимости

### Основные пакеты

- `auto_route: ^10.1.2` - декларативная навигация
- `provider: ^6.1.5+1` - управление состоянием
- `get_it: ^8.2.0` - внедрение зависимостей
- `flutter_secure_storage: ^9.2.4` - безопасное хранение
- `talker: ^5.0.0` - логирование

### Dev зависимости

- `build_runner: ^2.7.0` - генерация кода
- `auto_route_generator: ^10.2.4` - генератор роутов
- `flutter_lints: ^6.0.0` - линтер

